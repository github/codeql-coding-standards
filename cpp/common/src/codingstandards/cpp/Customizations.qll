/**
 * Contains customizations to the C++ Coding Standard query rules.
 *
 * This module is imported by the standard library for each Coding Standard, so any customizations
 * defined here automatically apply to all queries in all the C++ Coding Standards.
 */

import cpp
